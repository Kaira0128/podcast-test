name: Generate Podcast Feeds
on: [push]
jobs: 
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3
      
      - name: Run Feed Generator
        uses: kaira0128/podcast-generator@v1.0.0  # Cambia la versión

    env:
      ACTIONS_STEP_DEBUG: true
